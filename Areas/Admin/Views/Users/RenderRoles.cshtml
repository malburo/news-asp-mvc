@{
    Layout = null;
}

<div class="d-flex flex-wrap">
    @foreach (var item in ViewBag.UserRoles)
    {
        <button class="btn btn-primary m-1" style="padding: 5px 5px;">
          @item.Name
        </button>
    }
</div>