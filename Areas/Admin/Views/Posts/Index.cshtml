@model IEnumerable<NewsApplication.Models.Post>

@{
    ViewBag.Title = "Bài đăng";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Bài đăng</h2>

<p>
    <button class="btn btn-primary"> @Html.ActionLink("Tạo mới", "Create", null, new { @class ="text-white text-decoration-none"})</button>
</p>
<div class="card mb-4">
    <div class="card-header">
        <i class="fas fa-table me-1"></i> Các bài đăng
    </div>
    <div class="card-body">
        <table id="datatablesSimple">
            <thead>
                <tr>
                    <th>
                       Tiêu đề
                    </th>
                    <th>
                       Hình ảnh
                    </th>
                    <th>
                        Lượt xem
                    </th>
                    <th>
                        Ngày khởi tạo
                    </th>
                    <th>
                        Ngày cập nhập
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tfoot>
                <tr>
                    <th>
                        Tiêu đề
                    </th>
                    <th>
                        Hình ảnh
                    </th>
                    <th>
                        Lượt xem
                    </th>
                    <th>
                        Ngày khởi tạo
                    </th>
                    <th>
                        Ngày cập nhập
                    </th>
                    <th></th>
                </tr>
            </tfoot>
            <tbody>

                @foreach (var item in Model)
                {
                    <tr>

                        <td>
                            @Html.DisplayFor(modelItem => item.Title)
                        </td>
                        <td>
                            <img src="@item.ImageUrl" width="100" />
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Views)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CreatedAt)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.UpdatedAt)
                        </td>
                        <td>
                            <button class="btn btn-primary">@Html.ActionLink("Sửa", "Edit", new { id = item.PostId }, new { @class = "text-white text-decoration-none" }) </button>
                            <button class="btn btn-danger">@Html.ActionLink("Xóa", "Delete", new { id = item.PostId }, new { @class = "text-white text-decoration-none" }) </button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    </div>
