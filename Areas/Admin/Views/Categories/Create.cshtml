@model NewsApplication.Models.Category

@{
    ViewBag.Title = "Tạo";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Thể loại</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4> @Html.ActionLink("Thể loại", "Index", "", new { @class = "text-decoration-none" }) - Tạo </h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group pt-2">
            <label class="control-label col-md-2 pb-2">Tên</label>
            <div class="col-md-5">
                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group pt-2">
            <label class="control-label col-md-2 pb-2">Màu sắc</label>
            <div class="col-md-5">
                @Html.EditorFor(model => model.Color, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Color, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group pt-2">
            <div class="col-md-offset-2 col-md-10">
                <button type="submit" class="btn btn-primary">Tạo</button>
            </div>
        </div>
    </div>
}

<div class="" style="margin-top: 160px;">
    <button type="submit" class="btn btn-primary">@Html.ActionLink("Quay lại", "Index", null, new { @class = "text-white text-decoration-none" })</button>
</div>
